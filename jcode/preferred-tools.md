# Preferred tools

Use MCPProxy only for MCP: `http://localhost:8080/mcp/`.

For local work, prefer the built-in read, search, edit, and git tools. Use Fish-compatible commands whenever a shell command is necessary.
