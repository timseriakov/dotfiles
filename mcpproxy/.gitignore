# Runtime database
config.db

# Search index
index.bleve/

# Claude temporary files
.claude/

# macOS
.DS_Store

# Logs
*.log
main.log*

# Backup files
*.backup

# Note: mcp_config.json is committed but api_key should be removed before commit.
# Use fix-mcpproxy.sh --remove-api-key to clean it.
# Sensitive tokens are stored via keyring (${keyring:...}) and not committed.
