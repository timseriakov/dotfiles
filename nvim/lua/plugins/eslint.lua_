return {
	"MunifTanjim/eslint.nvim",
	event = { "BufReadPre", "BufNewFile" },
	config = function()
		require("eslint").setup({
			bin = "eslint",
			code_actions = true,
			diagnostics = true,
		})
	end,
}
